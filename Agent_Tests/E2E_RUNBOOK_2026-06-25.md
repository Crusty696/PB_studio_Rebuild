# E2E LIVE-RUN RUNBOOK — vorbereitet 2026-06-25 (zur Review VOR Ausführung)

Ziel: ein einziger durchgehender GUI-Live-Lauf auf AKTUELLEM Code:
PROJEKT → Import (Audio + ~127 Clips) → Audio-Analyse → Video-Analyse →
SCHNITT/Auto-Edit → EXPORT, jeder Schritt belegt (Screenshot + Log-Diff + DB-Delta).
Quelle: Agent_Tests/01..07 + GUI_PLAYBOOK_FROM_USER_RUN_2026-06-24 (516-Klick-Lauf).

Status: **VORBEREITUNG — NICHT AUSGEFÜHRT.** Erst nach deiner Freigabe + 3 Parametern.

---

## 0. HARNESS & UMGEBUNG (deterministisch, kein Blind-Klick)

- Python (PFLICHT, conda — `.venv310` fehlt laut Setup):
  `PY="%USERPROFILE%\miniconda3\envs\pb-studio\python.exe"`
- Harness: `tests/gui_harness.py` (JSON-Output, jedes `{"ok":false}` = STOP+Report)
  - Klick per Selektor: `click-element` / `find-element` (pywinauto invoke, objectName>Text)
  - Combo: `select-combo` · Eingabe: `type` · Taste: `key` · Slider/Spin: `set-value`
  - Beweise: `screenshot --label`, `log-since --offset N`, `find-crash --max 3`
- DB-Inspector (read-only): `tests/gui_db_inspector.py` → `counts`, `latest <table>`, `snapshot`, `query`
- GPU-Vorbedingung: `torch.cuda.is_available()==True` (war heute kurz „GPU lost"→Reboot; vor Start prüfen).
- Crash-Regel: jedes `UNHANDLED EXCEPTION|CRASH —|CRITICAL|Traceback|Internal C++ object ... deleted` im Log = **FAIL + STOP**.
- Ergebnis-Ordner: `Test-ergebniss/2026-06-25_e2e_<PROJEKT_NAME>/` (Screenshots, Logs, DB-Snapshots, ffprobe).

## 1. PARAMETER (Protokoll 01_Setup: IMMER frisch fragen, nie raten)

| Param | Kandidat (gefunden, von dir zu bestätigen) | Prüfung vor Start |
|---|---|---|
| `AUDIO_PFAD` | **offen** — Kandidaten: `…\Music\…\Zyce,_Querox_-_Feel_Free_(Original)_142__…Gb_Major….mp3` (1-10 min?) ODER der lange Crusty-DJ-Mix (>10 min, nur Stress-Check B-331) | existiert, .mp3/.wav/.flac, Länge messen (Soll 1-10 min) |
| `VIDEO_ORDNER` | `C:\Users\David_Lochmann\Videos\Solo_Natur-20260406T220640Z-3-001\Solo_Natur` → **127 .mp4 verifiziert** | existiert, MP4-Zahl melden |
| `PROJEKT_NAME` | Vorschlag `E2E_20260625` | noch nicht vergeben |

→ Bei Fehlprüfung: STOP, melden, nicht weiter.

## 2. SCHRITTE (deterministisch; je Schritt: Aktion → Wait → Screenshot → log-since → find-crash → DB-Delta → PASS/FAIL)

### S0 — Start
- `PY gui_harness.py start` (+ optional `--freeze-probe`) · `wait-window --title PB_studio --timeout 90`
- StartupCheckDialog: `click-element` Text="Weiter" (auch bei einer FAIL-Zeile)
- Screenshot `00_startup` · find-crash · **PASS:** 4 Tabs sichtbar (PROJEKT/MATERIAL & ANALYSE/SCHNITT/EXPORT)

### S1 — PROJEKT anlegen
- Nav: `click-element` objectName=`workspace_btn` Text="PROJEKT"
- `click-element` objectName=`btn_accent` Text="+ Neues Projekt" → NewProjectDialog
- Pfad-Picker "…", ComboBox; Name via `type` = `PROJEKT_NAME`; `click-element` objectName=`btn_ok` Text="Erstellen"
- Screenshot `01_projekt` · DB: `counts` → projects +1 · **PASS:** Projekt aktiv, kein Fehlerdialog
- **B-498-Check:** `ls storage/backups` → existiert `pb_studio_<datum>_daily.db`? (bekannt: oft NICHT) → in `01_backups.txt`

### S2 — Import Audio
- Nav `workspace_btn` "MATERIAL & ANALYSE" · Mode "AUDIO"
- `click-element` objectName=`btn_secondary` Text="+ Audio" → QFileDialog
- Im Dialog: `type` Pfad = `AUDIO_PFAD` → `key` ENTER (bzw. click-element "&Open")
- Screenshot `02_audio` · DB `counts` audio_tracks +1 · log-since · **PASS:** Track in Liste, korrekter Name, kein Crash

### S3 — Import Video-Ordner (~127)
- Mode "VIDEO" · `click-element` objectName=`btn_secondary` Text="+ Ordner" → Dialog → `type` `VIDEO_ORDNER` → "&Open"
- Wait: Proxy-Tasks (NVENC) laufen — poll `log-since` auf "proxy"/NVENC, KEIN "OpenEncodeSessionEx failed"
- Screenshot `03_clips` · DB `counts` video_clips == 127 · **PASS:** Clip-Zahl exakt, GUI bedienbar, kein Freeze/Crash
- **B-505-Watch** (Proxy-Parallelität), **B-219** (WinError32 file-lock)

### S4 — Audio-Analyse (Einzelschritte, `btn_secondary`)
- Reihenfolge: "Stems"→"Starten", "BPM / Beatgrid", "Wellenform", "Tonart", "LUFS", "Mood / Genre", "Spektral", "Songstruktur"
- Nach jedem: `log-since` + ProgressBar-Screenshot; warte auf "x% (n/8 Schritte)" → 8/8
- DB: beatgrids/waveform_data/structure_segments +rows; `ls storage/stems/<track>` → 4 .wav
- Screenshot `04_audio` · find-crash · **PASS:** 8/8 ohne Crash, Stems+Beatgrid+Struktur vorhanden
- **Watch:** B-507/510/524 (Stem-Hang/GPU-Lock), B-567 (Fehler ohne UI-Meldung)

### S5 — Video-Analyse
- Mode "VIDEO" · `btn_select_toggle` "Alle" · `btn_secondary` "Szenen" · `btn_accent` "Video komplett analysieren"
- Wait: lange Laufzeit (127 Clips × Scene/Motion/SigLIP/Keyframe). Poll `log-since`, GPU-VRAM via `find-crash`/log.
- DB: scenes +rows; Thumbnails sichtbar · Screenshot `05_video` · **PASS:** alle 127 analysiert, kein OOM/Mixed-Device
- **Watch:** B-572 (VRAM-Phantom 0 GB → Abbruch), GPU-1 (Device-Mismatch)

### S6 — SCHNITT / Auto-Edit (B-575 Crash-Stelle!)
- Nav `workspace_btn` "SCHNITT" · Empty-State Preset-Buttons (Techno/Cinematic/House/Festival)
- `click-element` Text="Techno" → Auto-Edit-Worker
- **B-575-Fokus:** danach `btn_accent` "Auto-Edit" bzw. "Timeline generieren" **mehrfach** klicken → Erwartung KEIN CrashDialog, kein "WaveformGraphicsItem already deleted"
- DB: timeline_entries (track=video) +rows; (falls Brain-Flag an: mem_pacing_run/mem_decision) ; log "Phase 3: N Segmente, N CutPoints"
- 1 Lock setzen (Lock-Icon) → Sub-Tab "Pacing & Anker" → "Re-Generate" → Confirm bestätigen → gelockter Clip bleibt
- Screenshot `06_schnitt` · find-crash · **PASS:** Timeline beat-synchron gefüllt, kein Crash, Lock erhalten
- **Watch:** B-553 (Clip-Drag bewegt nicht), B-NEU-1 (Inspector-Trim zerstört Timeline)

### S7 — EXPORT
- Nav `workspace_btn` "EXPORT" · `click-element` Text="Video exportieren"
- Wait: Fortschritt; log nennt `h264_nvenc` (oder dokumentierter Fallback libx264)
- Screenshot `07_export_laeuft` / `07_export_fertig` · **PASS:** abspielbare Datei in `exports/`
- ffprobe Zusatz-Beweis → `07_ffprobe.txt`: Video+Audio-Spur, Dauer ≈ Timeline-Länge
- **Watch:** B-541 (ffmpeg-8 NVENC-API + Treiber 546!), B-504 (Umlaut/Trim), FF-2 (pix_fmt)

### S8 — Gesamt-Konsistenz + Teardown
- Export-Video vs Timeline (Reihenfolge/Länge/Audio-Sync) · finaler DB-`snapshot`
- `PY gui_harness.py kill` (auch bei Abbruch — keine PID-Leichen)
- Report `Test-ergebniss/2026-06-25_e2e_<PROJEKT_NAME>/report.md` mit PASS/TEIL-PASS/FAIL-Tabelle + Bug-Liste (Traceback+Screenshot)

## 3. ABBRUCH-REGELN
- Erster Crash-Marker / `{"ok":false}` an kritischem Schritt → **STOP**, Screenshot + letzte 50 Log-Zeilen, Report, kill. Nichts beschönigen.
- Kein Schritt ohne Beweis = `UNVERIFIED` (nicht PASS).

## 4. WAS DU VOR FREIGABE ENTSCHEIDEST
1. `AUDIO_PFAD` final (welcher Track? 1-10 min empfohlen).
2. `VIDEO_ORDNER` bestätigen (Solo_Natur, 127 Clips) oder anderer.
3. `PROJEKT_NAME` bestätigen (`E2E_20260625`?).
4. Video-Komplett-Analyse von 127 Clips kann **lange** dauern (Minuten bis ~1 h je nach Stufen) — ok? Oder Teilmenge (z.B. 20 Clips) für schnelleren ersten Durchlauf?
