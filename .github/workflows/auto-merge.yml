name: 🤖 Auto-Merge Trusted PRs & Cleanup

# ==============================================================================
# B-720 (Supply Chain): Dieser Workflow hat vorher JEDEN PR automatisch per
# Squash gemerged und danach repo-weit Branches per API geloescht — ohne
# Autor-Filter und ohne Fork-Filter. Ein beliebiger Fork-PR konnte damit
# Auto-Merge angestossen bekommen.
#
# Zusaetzlich lief `cleanup-branches` mit `if: always()` und `needs: auto-merge`
# auch bei den Triggern `status` und `check_suite`, deren Payload gar keinen
# PR enthaelt: Auto-Merge wurde dann zum No-Op, die repo-weite Branch-Loeschung
# lief aber trotzdem durch.
#
# Absicherung (keine Deaktivierung, der Workflow bleibt fuer eigene PRs aktiv):
#   * Beide Jobs laufen nur, wenn der PR aus DIESEM Repo kommt (kein Fork)
#     UND der Autor OWNER / MEMBER / COLLABORATOR ist.
#   * `cleanup-branches` haengt an derselben Bedingung und laeuft nicht mehr
#     bei PR-losen Events.
# Anmerkung zur Ehrlichkeit: GitHub gibt Fork-PRs beim `pull_request`-Trigger
# ohnehin nur ein read-only GITHUB_TOKEN, der Fork-Merge war also praktisch
# schon blockiert. Der Autor-/Fork-Filter macht das explizit statt implizit und
# schliesst zusaetzlich die Branch-Loeschung mit ein.
# ==============================================================================

on:
  pull_request:
    types: [opened, synchronize, reopened]
  pull_request_review:
    types: [submitted]
  status: {}
  check_suite:
    types: [completed]

permissions:
  pull-requests: write
  contents: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    name: Auto-Merge PR
    # B-720: nur eigene (nicht-Fork) PRs von vertrauenswuerdigen Autoren.
    if: >-
      github.event.pull_request != null &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.pull_request.author_association)
    steps:
      - name: 📋 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Check PR Status
        id: check-pr
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            
            if (!pr) {
              console.log('No PR found in payload');
              return;
            }

            console.log(`PR #${pr.number}: ${pr.title}`);
            console.log(`State: ${pr.state}`);
            console.log(`Draft: ${pr.draft}`);
            console.log(`Mergeable: ${pr.mergeable}`);
            
            // Get PR details
            const prDetails = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
            });

            console.log(`Mergeable State: ${prDetails.data.mergeable_state}`);
            console.log(`Review Decision: ${prDetails.data.review_decision}`);

      - name: ⏳ Wait for Status Checks
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            
            if (!pr) return;

            let retries = 0;
            const maxRetries = 60; // 10 minutes max
            let allChecksPassed = false;

            while (retries < maxRetries && !allChecksPassed) {
              const prDetails = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
              });

              console.log(`Attempt ${retries + 1}/${maxRetries}`);
              console.log(`Mergeable State: ${prDetails.data.mergeable_state}`);

              if (prDetails.data.mergeable_state === 'clean' || prDetails.data.mergeable_state === 'has_hooks') {
                allChecksPassed = true;
                console.log('✅ All checks passed!');
              } else if (prDetails.data.mergeable_state === 'blocked' || prDetails.data.mergeable_state === 'behind') {
                console.log('⚠️ PR is blocked or behind. Skipping auto-merge.');
                process.exit(0);
              } else {
                console.log('⏳ Waiting for checks...');
                await new Promise(resolve => setTimeout(resolve, 10000)); // Wait 10 seconds
                retries++;
              }
            }

            if (!allChecksPassed) {
              console.log('⏱️ Timeout waiting for checks');
              process.exit(0);
            }

      - name: 🔗 Enable Auto-Merge
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            
            if (!pr) {
              console.log('No PR found');
              return;
            }

            try {
              const prDetails = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
              });

              // Check if already merged
              if (prDetails.data.merged) {
                console.log(`✅ PR #${pr.number} already merged`);
                return;
              }

              // Check if PR is in mergeable state
              if (prDetails.data.mergeable === false) {
                console.log(`❌ PR #${pr.number} has conflicts, cannot merge`);
                return;
              }

              // Enable auto-merge
              await github.rest.pulls.enableAutoMerge({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
                merge_method: 'squash',
              });

              console.log(`✅ Auto-merge enabled for PR #${pr.number}`);
            } catch (error) {
              console.log(`ℹ️ Auto-merge not available or already enabled: ${error.message}`);
            }

  cleanup-branches:
    runs-on: ubuntu-latest
    name: Cleanup Merged Branches
    needs: auto-merge
    # B-720: KEIN `always()` mehr. Vorher lief die repo-weite Branch-Loeschung
    # auch bei `status`/`check_suite`-Events ohne PR-Payload und bei Fork-PRs.
    # Jetzt dieselbe Trusted-Bedingung wie `auto-merge`.
    if: >-
      github.event.pull_request != null &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.pull_request.author_association)
    steps:
      - name: 📋 Checkout
        uses: actions/checkout@v4

      - name: 🧹 Delete Merged Branches
        uses: actions/github-script@v7
        with:
          script: |
            const branches = await github.rest.repos.listBranches({
              owner: context.repo.owner,
              repo: context.repo.repo,
              per_page: 100,
            });

            for (const branch of branches.data) {
              // Skip protected branches
              if (branch.protected) {
                console.log(`⛔ Skipping protected branch: ${branch.name}`);
                continue;
              }

              // Skip main and develop branches
              if (['main', 'master', 'develop', 'development'].includes(branch.name)) {
                console.log(`⛔ Skipping main branch: ${branch.name}`);
                continue;
              }

              try {
                const commit = await github.rest.repos.getCommit({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  ref: branch.name,
                });

                const branchInfo = await github.rest.repos.getBranch({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  branch: branch.name,
                });

                // Check if branch is merged into main
                const compareResult = await github.rest.repos.compareCommitsWithBasehead({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  basehead: `main...${branch.name}`,
                });

                if (compareResult.data.status === 'identical' || compareResult.data.total_commits === 0) {
                  await github.rest.git.deleteRef({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    ref: `heads/${branch.name}`,
                  });
                  console.log(`🗑️ Deleted merged branch: ${branch.name}`);
                }
              } catch (error) {
                console.log(`ℹ️ Could not process branch ${branch.name}: ${error.message}`);
              }
            }
