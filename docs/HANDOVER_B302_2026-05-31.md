# B-302 — Handover für Folge-Agent (frische Session)

> **Zweck:** B-302 (AutoEditWorker overloaded progress-Signal → TaskManager
> TypeError → native Access-Violation `0xC0000005`) sauber zu Ende analysieren
> und Fix-Entscheidung vorbereiten. **In frischer Session** — die Session vom
> 2026-05-31 hatte intermittierende Tool-Output-Korruption (fabrizierte
> Zeilen in Datei-Reads) + mehrere Rate-/Halluzinations-Fehler des Agenten.
>
> **Stand:** 2026-05-31 · Branch/Remote: `origin/main` = `3a1df64` · kein Code
> zu B-302 geändert · Bug-File `wiki/bugs/B-302-*.md` unverändert (71 Z, Original).

---

## 0. Wichtigste Regel — Ehrlichkeit (Hartregel)

- `status: fixed` setzt **nur der User**.
- Pfade **immer per Glob/Grep verifizieren** bevor Read/Bash. Nicht raten.
- Tool-Output (Read/Grep) **kann in langen Sessions korrupt sein** (fabrizierte
  Zeilen). Gegencheck: dieselbe Stelle 2× lesen; bei Widerspruch
  `import_smoke.py` für Datei-Validität nutzen; `git`-SHAs sind
  selbst-verifizierend = vertraubar.
- Kein Status-Eintrag im selben Tool-Batch wie die Aktion — **erst Ergebnis
  abwarten, dann loggen**.

---

## 1. Was B-302 ist (aus Bug-File, Original-Befund 2026-05-11)

App-Crash beim Klick auf Auto-Edit-Preset:

```
TypeError: 'PySide6.QtWidgets.QProgressBar.setValue' called with wrong argument types:
  PySide6.QtWidgets.QProgressBar.setValue(str)
→ Hard-Kill Exit -1073741819 = 0xC0000005 = STATUS_ACCESS_VIOLATION
→ Faulthandler: rekursive perf_watchdog._profiled_notify Frames (Slot-in-Slot Reentrancy → Heap-Korruption)
```

**Kausalkette laut Bug-File:** overloaded `progress`-Signal mit Default-Overload
`(str, float)` + nackter `.connect` (ohne Overload-Selector) → Slot bekommt
`pct=<stage_key:str>`, `msg=<fraction:float>` → `task.progress = "audio_load"`
→ `progress_bar.setValue(str)` → TypeError aus Qt-C++-Kontext (kein Python-except)
→ bei Wiederholung + paralleler GPU-Last → Reentrancy → Access-Violation.

**Bug-File nennt 2 dokumentierte Fixes (2026-05-11):**
1. `worker_dispatcher.py`: progress-Slot toleriert beide Overloads + coerced `pct` zu `int`.
2. `task_manager.py update_task`: defensive `int()`-Coercion für `progress`+`total`.

**Status laut Bug-File widersprüchlich** → siehe §4.

---

## 2. VERIFIZIERTE Fakten (mehrfach sauber gelesen, Stand 3a1df64)

> Diese Stellen wurden in der 05-31-Session mit konsistentem, korrektem
> git/Read-Format gelesen (kein Korruptions-Verdacht). Trotzdem in frischer
> Session **gegenlesen**.

| # | Datei:Zeile | Fakt |
|---|---|---|
| V1 | `workers/edit.py:24` | `progress = Signal((str, float), (int, str))` — overloaded, **Default = (str, float)** |
| V2 | `workers/edit.py:53` | Pipeline-Progress: `progress_cb=lambda pct, msg: self.progress[int, str].emit(pct, msg)` — **expliziter [int,str]-Selector → sicher** |
| V3 | `workers/edit.py` `_emit_stage` (≈Z.33-38, gerufen Z.50+56) | `self.progress.emit(stage_key, float(fraction))` — **nutzt Default-Overload (str, float)** → potentiell der gefährliche Pfad |
| V4 | `services/auto_edit_worker.py` | nur ~13 Zeilen = Shim (Klasse lebt in `workers/edit.py`) |
| V5 | `services/task_manager.py:284` | **nackter** `worker.progress.connect(lambda pct, msg, _tid: self.update_task(_tid, pct, message=msg), QueuedConnection)` — **kein** Overload-Selector |
| V6 | `ui/controllers/worker_dispatcher.py:67` | **nackter** `worker.progress.connect(lambda pct, msg, _tid: tm.update_task(_tid, pct, message=msg), qc)` — **kein** Selector |
| V7 | `services/task_manager.py:400-409` | `update_task`: `t.progress = progress` — **KEINE int()-Coercion** → dokumentierter Fix #2 ist **NICHT im Code** (kann roh einen `str` in `t.progress` schreiben) |
| V8 | `ui/widgets/task_manager_dock.py` (328 Z., aktiv, von `main.py` geladen) | `_on_task_updated` (Z.251) ruft `progress = _as_int(task.progress)` (Z.263) **vor** jedem `setValue`. |
| **V9** | `ui/widgets/task_manager_dock.py:13-20` `_as_int` | `isinstance`-Kette: `bool`/`int`/`float` → Zahl; **alles andere (inkl. `str`) → `return None`**. `"audio_load"` → `None`. Bei `progress is None` → `setRange(0,0)` (Z.265-266), **`setValue` wird NICHT mit str aufgerufen**. |

**SCHLUSS (verifiziert — Kette an der setValue-Stelle GESCHLOSSEN):**
- AutoEdit-**Pipeline**-Progress (V2) sicher (expliziter [int,str]).
- AutoEdit-**Stage**-Progress (`_emit_stage`, V3) feuert Default `(str, float)`;
  über nackten Connect (V5/V6) → `update_task(pct="audio_load", message=0.1)`
  → `t.progress = "audio_load"` (V7, str, keine Coercion).
- **ABER** der UI-Konsument `_on_task_updated` (V8) coerced via `_as_int` (V9):
  `str` ohne Zahl → `None` → `setRange(0,0)`, **kein `setValue(str)`** → kein
  TypeError → keine Reentrancy → kein `0xC0000005`.
- Der wirksame Schutz ist also die **Konsumentenseiten-Coercion** (`_as_int`,
  Kommentar B-128), **nicht** die im Bug-File dokumentierten Fixes #1/#2.
- **Verifik:** `pytest tests/ui/test_task_manager_dock_progress.py` = **2 passed**.
  Davon `test_b323_task_dock_ignores_non_numeric_progress` = direkter
  B-302-Regressionsschutz (non-numeric `progress` wird ignoriert, kein Crash).

**VERDIKT (Agent, statisch+unit):** B-302-Crash-Kette an der `setValue`-Stelle
**geschlossen** über `_as_int`. `status: fixed` setzt nur User nach Live-Lauf
(Akzeptanzkriterium §5).

---

## 3. RESTRISIKO — was eine Live-Verifikation noch klären sollte

> Die Crash-Kette ist statisch geschlossen (§V9, 16 Tests). Diese Punkte sind
> Rest-Sorgfalt, kein offener Blocker:

1. **Original-Crash war NATIVE `0xC0000005` unter Laufzeit-Last** (rekursiver
   `perf_watchdog._profiled_notify` bei wiederholtem Auto-Edit + paralleler
   GPU-Embedding-Last). `_as_int` verhindert den TypeError-Auslöser — aber
   echter Beweis = Live-Lauf gegen Akzeptanzkriterium §5 (18 Clips, 3 Re-Runs,
   0 `[CRITICAL]`). (Regressionstest `test_b323` deckt nur den TypeError-Auslöser
   statisch ab, nicht die native Reentrancy unter GPU-Last.)
2. **Kosmetik/Robustheit (optional, NUR mit User-OK):** `t.progress` kann via
   `update_task` einen `str` halten (V7). Aktuell harmlos (Konsument coerced),
   aber andere künftige Konsumenten von `task.progress` müssten `_as_int` auch
   nutzen. Defensive int()-Coercion in `update_task` (= dokumentierter Fix #2)
   wäre Gürtel-und-Hosenträger — **nicht nötig für B-302, nicht eigenmächtig**.
3. **Bug-File-Doku stale:** beschreibt Fix #1/#2 als die Lösung; tatsächlich
   wirkt `_as_int`. Doku angleichen (siehe §4).

---

## 4. Bug-File-Inkonsistenz (User/Folge-Agent klären)

`wiki/bugs/B-302-autoedit-progress-signal-overload-access-violation.md`:
- **Frontmatter:** `status: reverted-pending-alternative` (Fix war drin, **reverted**).
- **`## Status` am Ende:** `code-fix-pending-live-verification`.

Widerspruch. Vermutung: Fix wurde reverted (deshalb V7 = keine Coercion), aber
`## Status` nicht nachgezogen. **Git-Historie prüfen:**
`git log --oneline -- services/task_manager.py ui/controllers/worker_dispatcher.py`
nach B-302-Fix + Revert-Commits durchsuchen. Echten Stand feststellen, dann
Frontmatter korrigieren (status setzt User).

---

## 5. Constraints

- **Kein Fix ohne User-OK** (Status `reverted` → Re-Implementierung des
  Signal-Designs ist genau der reverted Teil). Erst Analyse + Repro, dann
  User fragen ob Fix #2 (Coercion) oder Selector-Variante.
- **GPU-Hartregel** (GTX 1060 / CUDA 11.3) gilt, falls Live-Auto-Edit getestet wird.
- **Akzeptanzkriterium (Bug-File):** Auto-Edit mit 18 Clips, 0 `[CRITICAL]
  UNHANDLED EXCEPTION`, TaskManagerDock zeigt numerische 0..100%, App überlebt
  3 Re-Runs ohne Hard-Kill.
- Vault-Pflicht pro Sub-Schritt (`C:\Brain-Bug\projects\pb-studio\log.md`).

---

## 6. Was die 05-31-Session belastbar erledigt hat (Kontext)

`origin/main` = `3a1df64` (ls-remote-bestätigt) enthält:
- F841 Cleanup services/ (`f6f0aa1` mfccs+perf_watchdog, `3a1df64`
  source_end×2 + except-e) — ruff passed, import_smoke 262/262, pytest 58 passed.
- `outputs/` gitignored (`c116a52`).
- Handover-Doc `docs/VERIFY_HANDOVER_2026-05-29.md`, FF-Merge refactor→feat, Push auf main.

B-302 selbst: **kein Code geändert, kein Verdikt** (Fix #2 fehlt im Code §V7,
Stage-Pfad ungeklärt §3). Suite-G-Rest weiter blockiert: B-331 (GPU-Live-Repro),
B-077 ("nicht implementieren" → User-OK), B-337 (User-Design Effekte-Tab).
